```markdown
# DBMS Day 33 - Window Functions Revision

## Topics Revised

- `AVG() OVER()`
- `PARTITION BY`
- `ROW_NUMBER()`
- `RANK()`
- `DENSE_RANK()`
- `LAG()`
- `LEAD()`
- Salary Difference
- `FIRST_VALUE()`
- `LAST_VALUE()`
- `NTH_VALUE()`
- Window Frames
- `UNBOUNDED PRECEDING`
- `UNBOUNDED FOLLOWING`
- CTE with Window Functions

## Quick Revision

| Function / Concept | Purpose |
|--------------------|---------|
| `avg() over()` | Overall average |
| `partition by` | Calculate separately for each group |
| `row_number()` | Unique row number |
| `rank()` | Ranking with gaps |
| `dense_rank()` | Ranking without gaps |
| `lag()` | Previous row value |
| `lead()` | Next row value |
| `first_value()` | First value |
| `last_value()` | Last value |
| `nth_value()` | Nth value |
| `rows between` | Controls the window frame |
| `cte` | Stores a temporary query result |

## Skills Revised

- Window Functions
- Ranking Data
- Comparing Rows
- Group-wise Calculations
- Window Frames
- CTEs
- Advanced SQL Queries

## Outcome
Today I revised the complete set of SQL Window Functions learned so far. I practiced averages, ranking, previous and next row comparisons, first/last/Nth values, window frames, and CTEs. This revision helped strengthen my understanding of advanced SQL analysis and row-based calculations.